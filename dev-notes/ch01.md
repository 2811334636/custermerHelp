# ch01 开发留痕 —— 电商智能客服系统 · 第一阶段（纯对话跑通）

记录规矩：每完成一个阶段就追记一段，记四样 ——
① 本步关键原话 ② 关键产出（spec/plan 路径、评审结论） ③ 被拒绝或纠偏的点 ④ 翻车与返工。
不攒到最后一次性补记。

---

## 阶段 0 · 启动（2026-09-18）

**① 关键原话**

> 我要使用Superpowers模式做一个电商智能客服系统,第一步先纯跑通对话
> 功能需求：对话接口 SSE 流式 / PromptTemplate 模板化 / with_structured_output 提取订单号+诉求期望+期望方案 / 多轮上下文最简版（消息剪裁 + token 预算）
> 技术栈：FastAPI + Python + Langchain，模型直连上游走 OpenAI 协议，deepseek/GPT/Kimi 可换着接
> 暂时不做：工具调用、Agent loop
> 验收：curl 看到流式；连问两轮第二轮能看到第一轮上下文；发售后描述能看到结构化 json
> 工作要求 1：全程走 Superpowers；非可单测产出（纯 Prompt、数据类）把 TDD 换成标注样例/评估集验证；**聊天页面例外，用 Vibe Coding，不套 brainstorm/TDD/code review**
> 工作要求 2：本文件留痕
> 工作要求 3：涉库/框架/API 一律先 Context7 查最新文档再动手
> 工作要求 4：技术选型定死，走不通停下来问，不要自行换
> 工作要求 5：完结交付演示命令 + 测试结果 + dev-notes 路径

**② 关键产出**

- 现场勘察结论：
  - `/home/zzx/custermerHelp` 空目录，仅有 `prompt.md`，**初始不是 git 仓库** → 已 `git init`，分支 `main`
  - Python **3.14.4**；有 `uv`，无 `pip3`
  - 环境存在可用上游凭证：`ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic`，key 前缀 `sk-8287…`（DeepSeek）
- 路径分类：**Architectural（架构级）**（全新项目、无既有流程可读），走完整流程：澄清 → 方案 → 分节设计 → spec → 用户审 → writing-plans
- spec 预计路径：`docs/superpowers/specs/2026-09-18-ecom-customer-service-chat-design.md`（待定稿）

**③ 被拒绝或纠偏**

- （暂无）

**④ 翻车与返工**

- `git init` 后 `git rev-parse --abbrev-ref HEAD` 因无提交报 `ambiguous argument 'HEAD'`。无影响，仅为无提交仓库的正常表现，首次 commit 后消失。

---

## 阶段 1 · 澄清 + 上游能力探测（2026-09-18）

**① 关键原话**

- 范围问题回答：「ch01 只做后端 API（推荐）」→ 聊天页面推到 ch02
- 结构化输出形态回答：「独立端点 /api/extract（推荐）」
- 会话存储回答：「进程内存字典 + Store 抽象（推荐）」
- 上游凭证回答：「复用现有的deepseek key,但是openai的协议端点应该为 `https://api.deepseek.com/` 模型为 deepseek flash」

**② 关键产出 —— 上游能力探测矩阵（这一阶段最有价值的产出，直接推翻/改写了多处设计假设）**

非流式：

| 能力组合 | 结果 |
|---|---|
| thinking ON + `tool_choice: required` | ❌ `Thinking mode does not support this tool_choice` |
| thinking ON + `tool_choice: {具体函数}` | ❌ 同上 |
| thinking ON + `tool_choice: auto` | ✅ 返回 tool_calls |
| thinking OFF + `tool_choice: required` | ✅ 返回 tool_calls |
| thinking OFF + `tool_choice: {具体函数}` | ✅ 返回 tool_calls |
| `response_format: json_schema` | ❌ `This response_format type is unavailable now`（两种模式都不行） |
| `response_format: json_object` | ⚠️ 报错要求 prompt 内含 "json" 字样 |

流式时序（同一客服问题，`max_tokens=400`，`stream_options.include_usage`）：

| | 首 reasoning chunk | 首 content chunk | content chunk 数 | reasoning_tokens |
|---|---|---|---|---|
| thinking ON | 0.76s | **从未出现** | **0** | 400（烧光全部预算） |
| thinking OFF | 无 | 0.55s | 400 | 无 |

关闭 thinking 的有效开关（探测到两个）：`"thinking":{"type":"disabled"}`、`"reasoning_effort":"none"`。
无效的：`"enable_thinking":false`、`"chat_template_kwargs":{"thinking":false}`（这两个实测仍会产生 reasoning_tokens）。

上游 `/models` 实际目录（2026-09-18）：`deepseek-flash`、`deepseek-v4-pro`。

Context7 查证结果（langchain 1.4.1 / langchain-core 1.6.3 / langchain-openai 1.6.2）：

- `ChatOpenAI(base_url=, api_key=, model=)` 支持自定义端点
- `with_structured_output` 的**默认 method 即 `function_calling`**，正好是上游唯一可行路径
- `trim_messages` / `count_tokens_approximately` 位于 `langchain_core.messages.utils`（**不是** `langchain_core.messages`）
- `ChatPromptTemplate.from_messages([("system", ...), MessagesPlaceholder("history")])` 形态成立
- 1.x 流式取文本用 `chunk.text`，**不是** 0.x 的 `chunk.content`

**③ 被拒绝或纠偏**

- 我原以为应默认用 `https://api.deepseek.com/v1`（OpenAI 惯用），用户纠正为 `https://api.deepseek.com/`。实测该路径 `/chat/completions` 正常工作。
- 我记忆笔记断言「deepseek-flash 不在模型目录里」，**已被实测推翻** —— `/models` 明确列出它。记忆已过期。

**④ 翻车与返工**

- 第一次探测用 bash 拼 JSON 字符串，引号嵌套导致 7b/7c/8 三组请求发出畸形 body、返回空响应，解析器抛 `JSONDecodeError`。**返工**：改写为 `/tmp/probe.py` 纯 Python 脚本重跑，一次性拿到完整矩阵。教训：涉及多层嵌套 JSON 的探测一律用脚本，不拼 shell 字符串。
- 最早那次 `max_tokens=20` 的探测返回空 content 且 `finish_reason=length`，当时误以为是参数问题，实为 thinking 吃光预算。**这个误判正是后来发现「thinking 必须关」的入口。**

---

## 阶段 2 · 设计定稿 + Spec 落盘（2026-09-18）

**① 关键原话**

- 架构选型回答：「A. 薄分层，不用编排（推荐）」
- 第 1 节（模块划分 + API 契约）确认：「对」
- AI 身份回答：「不主动说，问就承认（推荐）」
- 评估集强度回答：「正则 + LLM-judge（推荐）」
- 第 3 节（配置 + 错误处理 + 测试策略）确认：「ok,继续」

**② 关键产出**

- **Spec**：`docs/superpowers/specs/2026-09-18-ecom-customer-service-chat-design.md`
- 12 节结构，其中 §3「上游能力实测结论」为全文经验基础，§12 记录 5 项未决/风险（2 项标为**阻塞性**）
- 关键设计决策及其理由：
  1. **薄分层、不用 LCEL 管道** —— 上游三处非标约束（thinking、tool_choice、json_schema）让标准管道变成负担
  2. **`chat.py` 出领域事件，`api.py` 只封 SSE 帧** —— 对话逻辑可脱离 HTTP 单测
  3. **`APP_LLM_EXTRA_BODY` JSON 字符串透传** —— 避免把 DeepSeek 特有的 `thinking` 参数硬编码，换 provider 只改 `.env`
  4. **`start_on="human"`** —— 防止剪裁出「回答还在、提问没了」的孤儿消息
  5. **`meta` 事件承载 session_id** 而非响应头 —— curl 免 `-i`
  6. **断线不写入半截 assistant 历史** —— 否则污染下一轮上下文
- Spec 自查发现并**当场修复**一处：§4.1 只画了 `app/` 目录，但 §10/§11 引用了 `evals/` 和 `scripts/`，仓库根布局缺失；同时补上 uv 依赖管理与已验证的 Python 3.14.4 兼容结论
- 新增 `.gitignore`（首要目的是防止 `.env` 里的 key 进仓库）

**③ 被拒绝或纠偏**

- （本阶段用户全部采纳推荐项，无纠偏）

**④ 翻车与返工**

- 无。本阶段未产生返工。

**⑤ 本阶段仍阻塞的事项（带入实施计划 Task 0）**

1. `count_tokens_approximately` 对中文的估算偏差 —— 未标定，偏差 > 20% 需换可配计数器
2. `extra_body` 能否穿透 langchain-openai 把 `thinking` 送到上游 —— 未实测

---

## 阶段 3 · 实施计划落盘 + 自查返工（2026-09-18）

**① 关键原话**

> 给出实施计划吧

（承接上一轮的 spec 评审通过。用户未对 spec 提出修改。）

**② 关键产出**

- **Plan**：`docs/superpowers/plans/2026-09-18-ecom-customer-service-chat.md`，12 个任务，全部含可运行代码与测试代码
- 任务序列：1 骨架配置 → **2 阻塞性探测** → 3 数据模型 → 4 provider → 5 会话存储 → 6 上下文剪裁 → 7 Prompt → 8 对话服务 → 9 抽取服务 → 10 HTTP 层 → 11 评估集 → 12 验收脚本
- **计划对 spec 的两处修正**（已同步回 spec）：
  1. `APP_SESSION_MAX_TURNS` 拆成 `APP_SESSION_MAX_SESSIONS` + `APP_SESSION_MAX_MESSAGES`。原设计用单个"轮次上限"防内存增长，但**只限会话数并不阻止单个会话无限增长**——这是 spec 里的一个真实漏洞，写计划时才暴露
  2. 新增 spec §7.2.1「一轮对话的原子性」：`prepare_messages()` 的输入**不含**当前消息，当前消息由函数内部无条件追加。原设计若把当前消息一起丢进剪裁器，预算极小时会把用户提问本身剪掉 → 模型收到空提问
- Plan 自查覆盖表：spec 全部 18 个小节均映射到任务；无占位符；跨任务签名一致性逐条核对通过

**③ 被拒绝或纠偏**

- （本阶段用户尚未评审计划，暂无）

**④ 翻车与返工**

- **自查抓到一处自己写进计划的烂代码**：Task 8 的实现初稿用了 `prompt.format_messages(history=messages[1:]) if False else [...]` 这种故意绕开的写法，本意是"留个实现提示让执行者重写"，但**那就是把烂代码写进了计划**，而且掩盖了更干净的解法。返工后改为直接 `build_chat_prompt().format_messages(history=messages)`，一次成型，无需重写步骤。
- 该返工连带修正了两处依赖它的内容：`test_current_message_is_appended_to_prompt` 的断言（原断言漏算了 system 消息，会必然失败）、以及测试文件的 `SYSTEM_PROMPT` 导入。**教训：记录"计划里的代码也会被照抄执行"，不能留任何"等等再改"的写法。**

---

## 阶段 4 · 执行前 preflight 冲突扫描（2026-09-18）

**① 关键原话**

> 计划怎么执行？ → 用户选「Subagent 逐任务（推荐）」

**② 关键产出**

- 分支 `ch01-chat`（**裁决 1**：不建 worktree，改建特性分支）
- Ledger：`.superpowers/sdd/2026-09-18-ecom-customer-service-chat/progress.md`
- **Preflight 扫描出 5 处真实冲突**，全部裁决后才开工：
  1. `tests/conftest.py` 在 Task 1 的 Files 里列了却无步骤创建，真正创建点是 Task 4 → 归 Task 4
  2. `MetaEvent` 在 Task 8 被定义为产出但 `stream_reply` **从不产出它**，Task 10 却写了 `isinstance(event, MetaEvent)` 死分支 → 从 chat.py 删除该类型，api.py 删掉死分支（meta 是 HTTP 层职责，session_id 在 api.py 生成）
  3. Task 4 的测试断言 `m.openai_api_base` 属性名未经实机验证 → 若失败改断言不改生产代码
  4. Task 10 的 502 响应体形状（FastAPI 会包成 `{"detail": ...}`）→ 加 HTTPException 处理器摊平
  5. Task 6 的 `token_budget=1` 下 `trim_messages` 可能抛 `ValueError` → try/except 回退，不变量优先

**③ 被拒绝或纠偏**

- （暂无）

**④ 翻车与返工**

- （暂无）

---

## 阶段 5 · Task 1 项目骨架与配置 完成（2026-09-18）

**① 关键原话**

- 本轮无用户输入（Subagent 驱动执行，按计划自动推进）

**② 关键产出**

- commit `ebe5521`，`uv run pytest -v` → **4/4 通过，输出无警告**
- TDD 证据完整：RED = `ModuleNotFoundError: No module named 'app.config'`；GREEN = 4 passed
- 产出文件：`pyproject.toml` / `.env.example` / `.env` / `app/__init__.py` / `app/config.py` / `tests/__init__.py` / `tests/test_config.py` / `uv.lock`
- **安全验证（reviewer 独立复查了三次）**：`.env` 未被跟踪；`git check-ignore -v .env` → `.gitignore:2:.env`；`.env.example` 只含占位符 `sk-your-key-here`，无真实 key；提交信息尾部 trailer 精确匹配
- **Review 结论：✅ Spec compliant，Task quality Approved**，无 Critical、无 Important，7 条 Minor 全部标注 plan-mandated，已记入 ledger 待最终整支 review 分流

**③ 被拒绝或纠偏**

- （暂无）

**④ 翻车与返工**

- **reviewer 提了一条推理合理但实测证伪的 Minor**：它断言 `uv run uvicorn app.main:app` 会因 console script 的 `sys.path[0]=.venv/bin` 而导入失败，建议加 `[build-system]`。我**实测反驳**：`uvicorn/main.py:367-369` 的 `--app-dir` click 选项默认值是 `""` 而非 `None`，因此 `main.py:558-559` 的 `sys.path.insert(0, app_dir)` 确会执行，`""` 即 CWD。**没有据此改动任何代码。** 教训：reviewer 的结论也要验，不能因为它推导链条完整就照做。
- **裁决 8：Python 版本偏差**。`uv sync` 装出的是 **3.12.14**（uv 托管），不是机器的 3.14.4 —— 因为计划原文写的是 `requires-python = ">=3.12"`。裁决保留 3.12.14 并回改 spec §4.1 记录实际版本。理由：3.14.4 上的验证只是**依赖解析**层面，不是 LangChain 全链路运行验证，3.12 更稳。
- **裁决 9：review 包排除 `uv.lock`**。首次生成的 review 包 314KB，几乎全是锁文件，会把 reviewer 的 context 挤爆而毫无审查价值。改为手工生成排除锁文件的包，降到 4KB。

---

## 阶段 6 · Task 2 阻塞性探测 完成（2026-09-18）

**① 关键原话**

- 本轮无用户输入（Subagent 驱动执行，按计划自动推进）

**② 关键产出**

产出文件 `scripts/spike.py`（一次性探针，用后即弃，但仍提交以便复现）。**原始测量数字如下。** 结论中凡属"读库源码得出"的均已单独标注 **【读源码确认】**——那是钉死在 langchain-openai 1.6.2 / langchain-core 1.6.3 上的版本相关推断，**未经插桩验证，库升级后需重新核对**；只有**实测**部分可随时重跑复现。

**探测 1 —— `extra_body` 透传 `thinking`：✅ 通过**

```
=== 探测 1：extra_body 透传 thinking ===
  首个 text chunk: 1.01s
  文本 chunk 数: 22
  ✅ 结论：extra_body 透传成功，thinking 已关闭
```

三次重跑（首字延迟 / 文本 chunk 数）：**1.00s/20、1.01s/22、0.85s/22** —— 全部远低于 2.0s 判据，无一次接近阈值。

不止于延迟判据，另做了**线级直证**（logging transport 抓真实出站 HTTP body）：

- 对照组（不传 `extra_body`）出站 body：`{"messages": [...], "model": "deepseek-flash", "max_completion_tokens": 1024, "stream": true, "temperature": 0.3}` —— **没有 `thinking` 键**
- 实验组（传 `extra_body`）出站 body：同上 **+ `"thinking": {"type": "disabled"}`（顶层）**

即：`langchain-openai` 未吞掉该参数，`thinking` 确实抵达上游。**探测 1 的 ✅ 完全由上面这条出站 body 直证支撑。**

> **证据来源标注（review 后补，不涉及重跑）**
>
> 1. **"全程无 `reasoning_content` 泄漏" 这类辅助观察不具备区分力，不能当作"thinking 已关闭"的确认。** 对照组的那些空 text chunk 经查验是 `content=''`、`additional_kwargs={}`，但**对照组（不传 `extra_body`、thinking 未关闭）呈现完全相同的模式**。同一现象在两组都出现 ⇒ 无法区分 thinking 开与关。**因此本任务的探测 1 未做 thinking 开/关的行为对照，"思考是否真正关闭"只由 body 直证（送了什么）确认，未由响应行为（收到了什么）确认。**
> 2. **本文件「阶段 1 · 上游能力探测」的流式时序表（= spec §3.2）里有能区分开/关的对照数据**（设计阶段实测，非本次重测）：thinking ON 时"首 content chunk 从未出现、content chunk 数 0、reasoning_tokens 400（烧光全部预算）"；thinking OFF 时"无 reasoning chunk、content chunk 数 400"。本次探测 1 **没有**复现这个对照，故上述区分力缺口仍然存在。
> 3. **下面这句是读库源码得出的版本相关推断，属【读源码确认】而非【实测】**，未经插桩验证：`langchain_openai/chat_models/base.py:1488`（langchain-openai 1.6.2）把 `extra_body` 经 `_default_params` 作为具名 kwarg 交给 OpenAI SDK、由 SDK 摊平进 body 顶层。**库升级后此条需重新核对**，可重新测量的只有上一条出站 body。

**探测 2 —— 中文 token 估算偏差：❌ 大幅低估（ratio 0.42）**

```
=== 探测 2：中文 token 估算偏差 ===
  估算: 25
  实测: 59
  估算/实测 = 0.42
  ❌ 结论：低估超过 20% —— 阻塞，需换成显式 chars_per_token 的可配计数器
```

三次重跑，**估算 25 / 实测 59 / ratio 0.42 完全一致**（该数字可复现，非抖动）。

补充标定（把"低估"归因到具体常数）：

- 模板开销基线：空串消息 `input_tokens=4`；单字"好" `=5`；英文"hi" `=5` —— 固定开销约 **4 tokens**，而非 `count_tokens_approximately` 假定的 3
- 该段中文 `len=81` 字符，实测 `input_tokens=59` → 扣掉 4 token 开销后 **55 tokens / 81 字符 = 1.47 字符每 token**
- 把 `chars_per_token` 逐个**实跑代入 `count_tokens_approximately` 并打印**（脚本输出，非手算）：`4.0→25 (0.42)`、**`1.5→60 (1.02)`**、`1.2→74 (1.25)`、`1.0→88 (1.49)`

**结论：`langchain-core 1.6.3` 的 `chars_per_token` 默认值是 4.0（英文调优，此值系读已安装库签名确认，库升级后需复核），对中文低估 58%。** 计划给 Task 6 的兜底默认值 `chars_per_token=1.5` 与实测几乎完全吻合（估算 60 vs 实测 59），可直接采用。

**③ 被拒绝或纠偏**

- 未擅自改方案。探测 1 通过，无需触发备用路径（`model_kwargs` / `default_headers` / 给 openai SDK 打补丁）。
- 探测 2 判为 ❌ 但按 controller 判定**不算阻塞**（计划已在 Task 6 备好具名兜底），未在本任务内改动任何生产代码。

**④ 翻车与返工**

- **计划 Step 2 的运行命令跑不通**。`uv run python scripts/spike.py` → `ModuleNotFoundError: No module named 'app'`：`python <脚本路径>` 会把 `sys.path[0]` 设为**脚本所在目录** `scripts/`，而非仓库根，故 `app.config` 不可见。**未改脚本**（保持逐字忠实于计划），改为用等价命令 `uv run python -m scripts.spike` 运行（`-m` 会把 CWD 加入 `sys.path`），实测可跑通。这是**计划自身的命令与文件布局不匹配**，留待 controller 决定是否回改计划。
- **意外发现（不属本任务两个未决项，但必须上报）：`max_tokens` 在出站 body 里变成了 `max_completion_tokens`，而 DeepSeek 未据此截断，导致输出上限失效。** 实测证据（可重新测量）：
  - `ChatOpenAI(max_tokens=1024)` 出站 body 为 `"max_completion_tokens": 1024` → 实测 `output_tokens=1470`、`finish_reason='stop'`、正文 2454 字符 —— **未截断**
  - 改把上限塞进 `extra_body={"max_tokens": 1024}` → body 为 `"max_tokens": 1024` → 实测 `output_tokens=1024`、`finish_reason='length'`、正文 1710 字符 —— **精确截断**
  - **【读源码确认】**（langchain-openai 1.6.2，**未插桩验证** —— 本任务没有插桩证明该次请求确实经过这两处代码）：改名的落点疑为 `langchain_openai/chat_models/base.py:3703`（`_default_params`）与 `:3715-3718`（`_get_request_payload`）中**无条件**执行 `payload["max_completion_tokens"] = payload.pop("max_tokens")`，即 langchain 面向 OpenAI 侧新参数名的默认行为。**这是钉死在 1.6.2 版本上的推断，库升级后需重新核对。**
  - **【实测】可重新测量的部分**只有上面两条：两种构造下的出站 body 与对应的 `output_tokens` / `finish_reason`。由这两条可推出"'`max_completion_tokens`' 进了 body 而上游未据此截断"这一行为结论；**改名发生在哪一行代码，是读源码得出的，不是实测得出的。**
  - 影响 Task 4 的 `app/providers.py` 构造方式。**本任务未做任何修复**——用 `extra_body` 覆盖还是接受无上限，属产品取舍，须由人拍板。

---

## 阶段 7 · Task 2 评审与修复 + Task 3 数据模型（2026-09-18）

**① 关键原话**

- 本轮无用户输入（Subagent 驱动执行）

**② 关键产出**

**Task 2 评审结论：Needs fixes**（3 条 Important，**全是报告/留痕的证据来源标注问题**；无 Critical；**未重跑探测、未改任何数字**）

reviewer 的正面判词值得记下来，因为它说明了这个 spike 的实质是站得住的：probe 脚本逐字照抄；全部关键数字（81 字符、25/59=0.42、55→1.47、1.5→60/59、1470 vs 1024）**独立复算全部通过**；四点 `chars_per_token` 扫描在单一模型 `int((81+3)/cpt)+4` 下**精确复现**，证明是实跑而非编数；实现者未擅自修生产代码、未隐藏任何失败。

3 条 Important（已全部修复，commit `0cdef2a`）：

1. **读源码得出的结论与实测结论被放在同一证据等级**，且置于"结论均由上面的数字推出"标题之下（对该句**字面为假**）。修复：立三级约定【实测】/【读源码确认】/【推断】，并声明读源码那些**不是**探测结论的依据
2. **`report:141` 标题"失效机理确认"夸大**——该块只有出站 body 与 token 数，机理来自第 1 条的读源码论断
3. **probe 1 的"无 reasoning 泄漏"不具区分力**——对照组（thinking 未关）呈现**完全相同**的空 content 模式，两组皆有 ⇒ 无法区分开/关。**probe 1 的 ✅ 仅由出站 body 直证独立成立**，思考是否真正关闭**未经响应行为验证**

**Task 3 数据模型：commit `763c91d`**，7/7 通过，全量 11 passed，输出无警告
- **Review 结论：✅ Spec compliant，Task quality Approved**，无 Critical 无 Important，仅 2 条 Minor（`ExtractRequest.text` 的 8000 上界未被测试覆盖；description 只测存在不测内容）
- reviewer 指出 `test_ticket_field_descriptions_present` **不是**恒真测试（Pydantic 缺 description 时会省略该 key，故下标访问抛 `KeyError`）——这是它自己判断的，不是照抄实现者说法

**③ 被拒绝或纠偏**

- reviewer 的 3 条 Important 我**全部接受并要求修复**，未驳回任何一条。第 3 条尤其有价值：它指出"两组都观察到同一现象 ⇒ 该现象无区分力"，这是一条我先前漏掉的推理。**证据强度边界不是吹毛求疵，它是"我们到底证明了什么"的边界。**
- 实现者主动上报：spec §3.6① 里也有同一条不具区分力的表述，但它**没有越权改动我的文件**，而是留给我处置。**这个边界感是对的**，spec 已由我修正并加了⚠️证据强度边界段。

**④ 翻车与返工**

- **reviewer 报了一条 ⚠️ 说 report 写"Task 6"、brief 写"Task 7"，怀疑是笔误。查证结果：实现者是对的，计划是错的。** 计数器属于 `app/context.py`，那是 Task 6 的归属；brief 里那句 "Task 7" 是计划 Task 2 Step 3 的**遗留笔误**（`plans/...md:321`）。已修计划并注明"不是 Task 7"。**教训：reviewer 提出的"不一致"要先判哪边错，不能默认按 brief 改实现。**
- **reviewer 报 ⚠️「review 包不存在」——这次是我的流程失误**：我在 Task 3 的 review dispatch 里承诺了包路径，却没先跑打包脚本，只补了一句"若缺失请自行 fetch"。虽然 reviewer 自行重建后顺利完成任务，但这是把控制者的活推给了审查者。**后续每个 review dispatch 都必须先确认包已生成。**

---

## 阶段 8 · Task 4–7 完成（2026-09-18）

**① 关键原话**

- 本轮无用户输入（Subagent 驱动执行）

**② 关键产出**

| 任务 | commit | 测试 | Review 结论 |
|---|---|---|---|
| 4 Provider 工厂 | `9694077` | 5/5，全量 16 | ✅ Approved |
| 5 会话存储 | `6f2f0ad` | 6/6，全量 22 | ✅ Approved |
| 6 上下文剪裁 | `5b4b9e7` + fix `a556878` | 6/6，全量 28 | ✅ Approved（1 Important，见④） |
| 7 Prompt 模块 | `e44be8b` | 5/5，全量 33 | ✅ Approved |

**几个值得记下的高质量核验动作：**

- **Task 4 reviewer 去读了已安装的库源码**，确认 `assert m.max_tokens is None` **不是空断言**：`max_tokens: int | None = Field(default=None, alias="max_completion_tokens")`（`base.py:3660`），改名**只发生在构造请求体时**（`:3702-3703`、`:3717-3718`），**从不发生在属性上**。若用了被 Ruling 11 禁止的构造参数写法，该断言必失败 —— **这条回归防线是真的。**
- **Task 5 reviewer 对 LRU 测试做了反事实验证**：删掉 `move_to_end`，顺序保持 `[s1,s2]`，追加 s3 会淘汰 s1，断言必失败 ⇒ 该测试确实有区分力，实现者"靠推理不靠调参"的自述被证实。
- **Task 6 实现者机械核验了标定测试的非空性**：用库计数器同样剪裁保留 10 条 = 1066 token（超 `<=600` 断言必失败），用标定计数器保留 4 条 = 426 token（通过）。
- **Task 7 实现者用 sha256 机械核验提示词保真度**而非肉眼比对 —— 提示词文本就是本任务的交付物，这个做法对路。 reviewer 另做了一次**跨文件核对**：`EXTRACT_SYSTEM_PROMPT` 的诉求类别列表与 `AfterSalesTicket.demand` 的字段描述**逐字节一致**（这两个在抽取路径上一起被消费，漂移会让模型被误导）。

**③ 被拒绝或纠偏**

- **Task 4 实现者报了一条 brief 自相矛盾**：计划原有测试断言 `m.extra_body == {"thinking": {...}}`（整字典相等），而 Ruling 11 强制生产代码用 `setdefault` 注入 `max_tokens`，两者不可能同时成立 —— **这是我修改计划时引入的缺陷**。它没删断言、没改生产代码，把断言收窄为 `m.extra_body["thinking"] == {...}` 保住原意。**裁决 19：确认其处置，不回退**，并据此修正计划原文。
- **Task 6 实现者另做一处刻意偏离**：计划的 `git add` 漏了被修改的 `app/config.py`，它补上了（否则提交后的树 import 时 `AttributeError`）。处置正确。

**④ 翻车与返工**

- **Task 6 review 的一条 Important 直接指向我自己的 Ruling 5**。reviewer 去读 `langchain_core/messages/utils.py` 源码确认：本调用配置下仅有的 `ValueError` 都是**参数校验**（`:1442-1489`），**无一与预算相关**；深层 raise 仅在 `allow_partial=True` 时可达；`_last_max_tokens` 路径（`:1508-1518`、`1970+`）**返回 `[]` 而非抛错**。所以那段 `except ValueError` **不可达**，**且其注释在陈述一个不会发生的机理**——对维护者构成误导。**裁决 24：保留分支（它兜住的是"当前消息永不被剪裁"这条最重要不变量，优雅降级优于整个请求 500），但注释必须改为陈述实测事实。** 已 fix 并同步修正计划原文。
- **我自己的流程失误（第二次）**：我在 Task 8 的 dispatch 里写了"Task 10 的 brief 已同步修正（Ruling 2）"，**但实际上我还没改计划**——如果没立刻补上，那句话就是假的，而 Task 10 的实现者会照着一个自相矛盾的 brief 干活。已立即补改：从计划的 Task 8 定义与 Task 10 引用中移除死代码 `MetaEvent`，重新生成 brief 并确认 brief 中残留为 0。**教训：对 subagent 陈述的事实必须当场为真，不能"打算随后补"。**

---

## 阶段 9 · Task 11 评估集（2026-09-18）

**① 关键原话**

- 用户要求 1：「不是单元可测的东西（纯提示词文本、数据），**用跑标注样本集/评估集的替代 TDD**」。本任务即该要求的落点。

**② 关键产出**

最终成绩：**Prompt 行为 15/15，抽取字段级 36/36 = 100%**（连跑两次同分，exit 0）。全量单测仍 59 passed。

迭代轨迹（每轮 ~30 次真实 API 调用）：

| 轮次 | 改了什么 | Prompt | Extract |
|---|---|---|---|
| run-0 | brief 脚本**逐字**照抄 | 3/15 | 29/36 |
| run-1 | 修 harness：回复改成真的带 system prompt | 14/15 | 29/36 |
| run-2 | 改 SYSTEM_PROMPT 能力边界 + 重写 EXTRACT_SYSTEM_PROMPT | 14/15 | **35/36** |
| run-3 | 修 fabricate-2 正则误伤 | **15/15** | 35/36 |
| run-4 | EXTRACT 加压缩示例 | 15/15 | **36/36** |

**brief 的脚本有一处硬伤**：`run_prompt_case` 是 `model.ainvoke([HumanMessage(input)])`，**模型根本收不到 `SYSTEM_PROMPT`**。逐字照抄跑出来的 3/15 里，`outofscope-1` 直接吐出了完整的淘宝爬虫脚本（命中 `import `/`def `/`requests.get`）。改成 `build_chat_prompt().format_messages(history=[...])`（= `app/chat.py:70` 的生产路径）后**一步从 3/15 到 14/15**。**这个 harness 缺陷不是小事：不修的话，本任务测的根本不是本任务要测的东西。**

**归因不能靠单次运行**：实测同一份代码连跑两次，15 条回复里**有 14 条文本不同**（上游 temperature=0 并不确定）。所以「改措辞→用例翻绿」全部另做 A/B 重复验证：
- `fabricate-3`：旧措辞命中禁用词 **3/3**，新措辞 **0/3** —— 是措辞改的，不是抖动
- `ex-11`：无示例 `换个颜色` **0/3**，有示例 `换颜色` **3/3**（三次输出完全相同）
- 顺带量出：**抽取路径（structured output）是稳定的，抖动只出现在自由文本回复路径**

**③ 被拒绝或纠偏**

- **只改了 1 条用例（`fabricate-2` 的禁用正则），并已上报**。原 `在.{2,8}市` 会把**拒绝语**"我没法告诉你快递**在哪个城市**"判成编造（`在`+`哪个城`+`市` 命中）。加否定前瞻 `在(?!哪|什么|几)[一-龥]{2,8}市` 后，编造样本"在杭州市中转""在北京市的网点""在武汉市的转运中心"**仍全部命中** —— 区分力未降，误伤消除。**这是修量具，不是把用例掰弯。**
- **六条行为约束一条没删**。SYSTEM_PROMPT 的改动只是把约束 1 从"不能说你查到了"扩到"也不能替用户说出具体状态名"（`fabricate-3` 的失败正是模型在列举"退款成功/已退款"这些状态标签）。

**④ 翻车与返工**

- **我自己写的 brief 里的脚本是错的**，而且错在最要命的地方——它让被测对象拿不到被测的提示词。**教训：评估脚本自身必须先被验证"测的是它声称要测的东西"**；run-0 的 3/15 差点被当成"提示词质量差"的结论。
- **遗留未覆盖项**：约束 4「单次回复 ≤150 字」**没有任何用例覆盖**，实测 `overreach-3` 一条回复 154 字（超 4 字）。用例集是给定的、不得增删，故仅上报不擅自加用例。

### 阶段 9b · Task 11 评审修复（Important：约束 4 零覆盖）

**评审结论 Approved，一条 Important**：约束 4 零覆盖且存在实测违规 —— 本任务是**唯一**的行为闸门，`15/15` 因此**高估了机器人的合规度**。**这条接受，且实际情况比评审看到的更广。**

**① 先量问题，再动手**

A/B（4 条最长输入 × 3 次）：旧约束 4 措辞 **超限 2/12（最长 151，均值 122）**，新措辞 **0/12（最长 106，均值 79）**。**154 字不是一次巧合，是均值贴着上限跑出来的尾巴。**

**② 三处改动**

1. **harness 加长度闸门**（纯增量）：`MAX_REPLY_CHARS = 150` 常量（注释写明"就是约束写的那个数，不因模型做不到而下调"）+ `run_prompt_case` 计入 `too_long` + 超长时打印**实际字数/超出多少/回复原文** + 汇总行。阈值 150 一个数没动。
2. **约束 4 措辞**：`不超过 150 字` → `控制在 100 字以内（150 是硬上限，任何情况下都不得突破）`。**目标值降到 100，硬上限仍是 150** —— 给抖动留余量，不是放宽约束。
3. **回复风格两句**：投诉要给明确渠道；无关请求要明确拉回。

**③ 加闸门当场暴露了"15/15 本来就不稳"**

run-6：`emotion-3` 掉 judge，`overreach-3` 反而 148 字。**首轮那个 15/15 是运气好的一次采样。**

`outofscope-3` 的诊断尤其有价值，它把"模型问题"和"judge 问题"分开了：
- **同一段被判否的回复送 judge 5 次 → 5/5 判否**（judge 对给定文本是**稳定**的，不是掷硬币）
- 重新生成 4 条 → 3 条通过、1 条判否；判否那条结尾是"想听正经的，还是继续唠？"，通过的那几条结尾都**点名了能帮什么**

**所以抖动在生成侧，不在评判侧。**修法是把"明确拉回"写成硬要求，而不是去改 judge 或用例。

**④ 老实说：这两条风格改动没有干净的 FAIL→PASS 归因**

A/B 里 `emotion-3` 新旧都 4/4 通过、`outofscope-3` 新旧都 5/5 通过。**我不宣称它们"把某条用例从 FAIL 变成 PASS"。**可量化的收益是两处：`emotion-3` 回复里明确给出渠道的比例 **2/4 → 4/4**（行为变稳定），越界类回复长度 **59~66 → 36~45 字**（服务于约束 4）。

**⑤ 结果**：连跑三次 `Prompt 15/15`，长度断言 **0/45 超限**，`overreach-3` **154 → 94 字**（余量 −4 → +56），抽取 36/36，全量单测 59 passed。

---

## 阶段 10 · Task 12 验收脚本与文档（2026-09-18）

**① 关键原话**

- 用户要求 1：「本章结束时必须能用 curl 演示三条：流式输出、多轮上下文、结构化 json」——本任务即该要求的最终落点。
- 派工原话：「**"我写了脚本" 和 "脚本跑过了" 是两件事，只有第二件算数**。」

**② 关键产出**

- `scripts/smoke.sh`（可执行）、`README.md` 按 brief 逐字落盘，`sha256` 机械核验与 brief 一致（smoke `63da00e1…`，README `13642898…`）。
- 真实服务 + 真实上游跑通，三条验收标准均**有可见输出**，验收 2 打印 `✅ 验收 2 通过`。
- `uv run pytest -v`：**59 passed**；`uv run python -m evals.run_evals`：**Prompt 15/15，Extract 36/36**（独立复现 Task 11 的成绩，exit 0）。
- README 命令逐条实测：`uv sync` exit 0；`uv run uvicorn …` 真的起了服务；`bash scripts/smoke.sh` 见下。

**③ 验收 2 的断言是坏的 —— 这是本任务唯一的实质发现**

第一次跑 brief 的脚本，验收 2 打印 `❌ 验收 2 失败`。但同一份输出里**拼回来的回复正文清清楚楚写着** `您刚才说的是 A12345。` —— **模型行为是对的，断言判错了。**

抓原始帧定位到机理：上游按 token 切分 delta，订单号被切成三个**独立帧**：

```
data: {"text": " A"}
data: {"text": "123"}
data: {"text": "45"}
```

而 brief 的脚本 `printf '%s' "$R2" | grep -q 'A12345'` grep 的是**原始 SSE 流**，连续字节 `A12345` 从未出现 ⇒ 必然误判。派工说明里把这个断言描述成「greps round two's **reply**」，**脚本实现与它自己的描述不一致**。

处置：**只改断言的对象，不改断言的语义** —— 把拼好的正文存进 `REPLY`，改成 `grep 'A12345' "$REPLY"`。同样的字面量、同样的问句，只是从「在帧流里找连续字节」改成「在回复正文里找」。`diff` 全程只有这一处 hunk，已留存。

**区分力反事实验证（关键）**：光说"改完就绿了"不足以证明这不是把用例掰弯。补做**负对照**——开一个**全新会话**（无历史）问同一句「我的订单号是多少？」，模型答「我这边看不到您的订单信息哦…」，**修改后的断言正确判失败**。⇒ 该检查在上下文没生效时**仍会红**，区分力保留，不是软化。

**④ 翻车与返工**

- **brief 的脚本硬伤（第二次同类）**：Task 11 是评估脚本拿不到 SYSTEM_PROMPT，本任务是断言对象搞错。**共同点：brief 里的脚本从未被真正跑过。** 教训与 Task 11 一致 —— **脚本自身必须先被验证「测的是它声称要测的东西」。**
- **偏差已上报**：派工要求脚本逐字使用，本任务对 1 处 hunk 做了必要修正，理由与证据均在上；若 controller 裁决不接受，回退该 hunk 即可（`git revert` 该提交的单块）。
- **未执行 brief Step 6（发起 code review）**：本任务派工明确禁止派发 subagent，Step 6 由 controller 侧另行安排。
- **`cp .env.example .env` 未实跑**：会覆盖仓库里正在工作的 `.env`（真实 API key）。改为**等价非破坏性验证**：把 `.env.example` 逐字拷成临时目录的 `.env`，确认 `Settings()` 能加载（含 `extra_body` 解析为 `{'thinking': {'type': 'disabled'}}`）且 `app.main` 可导入。结论：快速开始那一行命令是好的，只差填 key。

**⑤ 原始输出（`bash scripts/smoke.sh`，逐字）**

```text
======================================================================
验收 1：curl 对话看到流式输出（观察 delta 是否逐条到达）
======================================================================
event: meta
data: {"session_id": "277fcd13-a664-44ed-95d7-693c04b68c3e"}

event: delta
data: {"text": "换"}

event: delta
data: {"text": "货"}

event: delta
data: {"text": "没问题"}

event: delta
data: {"text": "～"}

event: delta
data: {"text": "您"}

event: delta
data: {"text": "可以在"}

event: delta
data: {"text": " App"}

event: delta
data: {"text": " "}

event: delta
data: {"text": "里"}

event: delta
data: {"text": "这样"}

event: delta
data: {"text": "操作"}

event: delta
data: {"text": "：\n\n"}

event: delta
data: {"text": "1"}

event: delta
data: {"text": "."}

event: delta
data: {"text": " "}

event: delta
data: {"text": "打开"}

event: delta
data: {"text": "「"}

event: delta
data: {"text": "我的"}

event: delta
data: {"text": "订单"}

event: delta
data: {"text": "」，"}

event: delta
data: {"text": "找到"}

event: delta
data: {"text": "这"}

event: delta
data: {"text": "双"}

event: delta
data: {"text": "鞋"}

event: delta
data: {"text": "的"}

event: delta
data: {"text": "订单"}

event: delta
data: {"text": "\n"}

event: delta
data: {"text": "2"}

event: delta
data: {"text": "."}

event: delta
data: {"text": " "}

event: delta
data: {"text": "点"}

event: delta
data: {"text": "「"}

event: delta
data: {"text": "申请"}

event: delta
data: {"text": "售后"}

event: delta
data: {"text": "」"}

event: delta
data: {"text": "→"}

event: delta
data: {"text": " "}

event: delta
data: {"text": "选"}

event: delta
data: {"text": "「"}

event: delta
data: {"text": "换"}

event: delta
data: {"text": "货"}

event: delta
data: {"text": "」\n"}

event: delta
data: {"text": "3"}

event: delta
data: {"text": "."}

event: delta
data: {"text": " "}

event: delta
data: {"text": "按"}

event: delta
data: {"text": "提示"}

event: delta
data: {"text": "填"}

event: delta
data: {"text": "好"}

event: delta
data: {"text": "要"}

event: delta
data: {"text": "换"}

event: delta
data: {"text": "的"}

event: delta
data: {"text": "尺"}

event: delta
data: {"text": "码"}

event: delta
data: {"text": "，"}

event: delta
data: {"text": "提交"}

event: delta
data: {"text": "就行"}

event: delta
data: {"text": "\n\n"}

event: delta
data: {"text": "提交"}

event: delta
data: {"text": "后"}

event: delta
data: {"text": "具体"}

event: delta
data: {"text": "进度"}

event: delta
data: {"text": "以"}

event: delta
data: {"text": "订单"}

event: delta
data: {"text": "页"}

event: delta
data: {"text": "显示"}

event: delta
data: {"text": "为准"}

event: delta
data: {"text": "。"}

event: delta
data: {"text": "如果"}

event: delta
data: {"text": "找不到"}

event: delta
data: {"text": "入口"}

event: delta
data: {"text": "或"}

event: delta
data: {"text": "操作"}

event: delta
data: {"text": "卡"}

event: delta
data: {"text": "住"}

event: delta
data: {"text": "，"}

event: delta
data: {"text": "我"}

event: delta
data: {"text": "帮"}

event: delta
data: {"text": "您"}

event: delta
data: {"text": "转"}

event: delta
data: {"text": "人工"}

event: delta
data: {"text": "客服"}

event: delta
data: {"text": "处理"}

event: delta
data: {"text": "～"}

event: done
data: {"finish_reason": "stop", "usage": {"input_tokens": 262, "output_tokens": 84, "total_tokens": 346, "input_token_details": {"cache_read": 128}, "output_token_details": {}}}



======================================================================
验收 2：连续两轮，第二轮必须能看到第一轮的上下文
======================================================================
第一轮 session_id = 865b0a40-5604-4ae9-a243-547e752b1e51
第二轮回复：
您刚才说的是 A12345。

不过这个号我这边查不了，具体信息还得您在 App「我的订单」里核对一下哈。您遇到啥问题了？
✅ 验收 2 通过：第二轮回复中出现了第一轮给出的订单号 A12345

======================================================================
验收 3：发一段售后描述，看到结构化 json
======================================================================
{
    "order_id": "A123",
    "demand": "\u9000\u6b3e",
    "expected_solution": "\u539f\u8def\u9000\u56de"
}
```
